# Self-signed SSL for local HTTPS (e.g. https://localhost:8443)
# 1. Generate cert: from project root, run ssl-selfsigned/generate-self-signed.sh (or .ps1)
# 2. Run with: spring.profiles.active=mysql,selfsigned (or your DB profile + selfsigned)

#server.port=8443
#server.ssl.certificate=file:./ssl-selfsigned/cert.pem
#server.ssl.certificate-private-key=file:./ssl-selfsigned/key.pem

# Base URL for links (use HTTPS when using this profile)
#app.api.base-url=${API_BASE_URL:https://localhost:8443}
